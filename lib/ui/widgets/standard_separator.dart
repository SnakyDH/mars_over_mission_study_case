import 'package:flutter/material.dart';

class StandardSeparator extends StatelessWidget {
  const StandardSeparator({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 40);
  }
}
